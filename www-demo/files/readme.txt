webserv demo files
==================

This folder is served as static content by the webserv HTTP server.
If autoindex is enabled on this route and no index file is present,
you should see a generated directory listing instead of a 403/404.
